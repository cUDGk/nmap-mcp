{
  "name": "nmap-mcp",
  "version": "0.2.1",
  "description": "MCP stdio server wrapping nmap — host discovery, port scanning, service detection with structured XML→JSON output",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "nmap-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.8",
    "fast-xml-parser": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
